# نشر سفنز درايف على Render (الباقة المجانية)

خطوات نشر المشروع من GitHub إلى Render على الخطة المجانية.

---

## 1) الدخول إلى Render وربط GitHub

1. ادخل إلى **https://render.com** وسجّل الدخول (أو أنشئ حساباً).
2. من لوحة التحكم اضغط **New +** ثم اختر **Web Service**.
3. في **Connect a repository** اختر **GitHub** وربط حسابك إن لم يكن مربوطاً.
4. اختر المستودع: **hassancs44/se_sheetsai** (أو الاسم الصحيح لمستودعك).

---

## 2) إعداد الخدمة (مطابق للصورة)

| الحقل | القيمة |
|--------|--------|
| **Name** | `se_sheetsai` (أو أي اسم تريده) |
| **Region** | Oregon (US West) أو الأقرب لك |
| **Branch** | `main` |
| **Root Directory** | اتركه فارغاً |
| **Runtime** | **Docker** (Render يكتشف الـ Dockerfile تلقائياً) |
| **Instance Type** | **Free** — 512 MB RAM، 0.1 CPU |

تحذير الباقة المجانية:
- الخدمة تتوقف بعد فترة عدم استخدام (تصبح نشطة عند الطلب).
- لا قرص ثابت: قاعدة SQLite تُمسح عند إعادة التشغيل (البيانات غير دائمة).
- لا SSH ولا Scaling.

---

## 3) متغيرات البيئة (Environment Variables)

من قسم **Environment** اضغط **Add Environment Variable** وأضف التالي:

| الاسم | القيمة | ملاحظة |
|--------|--------|--------|
| `SECRET_KEY` | سلسلة عشوائية طويلة | مثلاً: `my-super-secret-key-2026-change-me` |
| **`ADMIN_EMAIL`** | **بريدك الإلكتروني** | **مطلوب للتسجيل** — على Render لا يوجد ملف Excel للمستخدمين، فيُستخدم هذا البريد للدخول |
| **`ADMIN_PASSWORD`** | **كلمة مرور المدير** | **مطلوب للتسجيل** — كلمة المرور التي تدخل بها في صفحة تسجيل الدخول |
| `BASE_URL` | **اتركه فارغاً في البداية** | بعد أول نشر، ضع رابط الخدمة (مثل `https://se-sheetsai.onrender.com`) |
| `ONLYOFFICE_JWT_SECRET` | أي نص سري | مطلوب إذا ضبطت OnlyOffice (نفس القيمة في خادم OnlyOffice) |
| `ONLYOFFICE_SERVER` | **مطلوب للتحرير** | إن أردت OnlyOffice إجباري: أضف خدمة OnlyOffice ثانية على Render أو استخدم خادم OnlyOffice خارجي وضعه هنا (مثل `https://onlyoffice-xxx.onrender.com`) |
| `ONLYOFFICE_REQUIRED` | `true` أو اتركه | إذا `true`: التطبيق **لا يبدأ** إلا إذا كان `ONLYOFFICE_SERVER` مضبوطاً (OnlyOffice إجباري وتشغيله من السيرفر). للباقة المجانية بدون OnlyOffice اتركه فارغاً أو `false` |
| `GUNICORN_WORKERS` | `1` | مهم للباقة المجانية (ذاكرة 512 MB) |

**بعد أول نشر ناجح:**
- انسخ رابط الخدمة (مثل `https://se-sheetsai-xxxx.onrender.com`).
- أضف متغيراً: `BASE_URL` = هذا الرابط بالضبط (بدون / في النهاية).
- من القائمة: **Manual Deploy** → **Deploy latest commit** لإعادة النشر مرة واحدة.

---

## 4) النشر

1. اضغط **Create Web Service**.
2. انتظر حتى ينتهي الـ Build ثم الـ Deploy (قد يستغرق بضع دقائق).
3. عند النجاح سيظهر رابط الخدمة في أعلى الصفحة (مثل `https://se-sheetsai-xxxx.onrender.com`).

---

## 5) بعد أول نشر

1. ادخل إلى **Environment** في الخدمة.
2. أضف أو عدّل: **`BASE_URL`** = رابط الخدمة (مثل `https://se-sheetsai-xxxx.onrender.com`).
3. من تبويب **Manual Deploy** اختر **Deploy latest commit**.
4. بعد انتهاء النشر جرّب فتح الرابط وتسجيل الدخول.

---

## تسجيل الدخول على Render

على السيرفر **لا يوجد ملف** `data/database.xlsx` (غير مرفوع في المستودع لأسباب أمان). لذلك يجب تعريف مدير واحد عبر متغيرات البيئة:

- **`ADMIN_EMAIL`** = البريد الذي تسجّل به (مثل `admin@example.com`)
- **`ADMIN_PASSWORD`** = كلمة المرور التي تدخلها في صفحة الدخول

بدون هذين المتغيرين ستظهر رسالة **«بيانات الدخول غير صحيحة»** حتى لو كانت البيانات صحيحة محلياً.

---

## ملاحظات الباقة المجانية

- **قاعدة البيانات:** التطبيق يستخدم SQLite افتراضياً. على Render المجاني القرص غير ثابت، فبيانات المستخدمين والملفات تُفقد عند إعادة تشغيل الخدمة. للاستخدام الجاد يمكن لاحقاً إضافة **PostgreSQL** من Render (لديهم خطة مجانية محدودة) وتعديل التطبيق لاستخدامها.
- **OnlyOffice:** تحرير الإكسل يحتاج خادم OnlyOffice. لجعله **إجبارياً**:
  - ضع `ONLYOFFICE_REQUIRED=true` و `ONLYOFFICE_SERVER` = عنوان خادم OnlyOffice (خدمة ثانية على Render أو سيرفر خارجي).
  - بدون OnlyOffice يمكن تشغيل التطبيق بدون تحرير (عرض/تنقل فقط) — لا تضبط `ONLYOFFICE_REQUIRED` أو ضبطه `false`.
- **النوم (Spin down):** عند عدم زيارة الموقع مدة معينة تتوقف الخدمة؛ أول طلب بعدها قد يأخذ 30–60 ثانية حتى تعود الخدمة.

---

## استكشاف الأخطاء

- **Build فاشل:** تأكد أن الفرع هو `main` وأن المستودع هو `hassancs44/se_sheetsai` (أو اسم مستودعك الصحيح).
- **Application failed to respond:** تأكد أن `GUNICORN_WORKERS=1` مضبوط، وأنه لا توجد متغيرات بيئة خاطئة.
- **صفحة بيضاء أو 502:** بعد النشر انتظر دقيقة ثم حدّث. إن استمر، راجع **Logs** في Render وتحقق من وجود `SECRET_KEY` و`BASE_URL` بعد أول نشر.
